import { View, Text } from "react-native";
import React from "react";

const BookStoreItems = () => {
  return (
    <View>
      <Text>BookStoreItems</Text>
    </View>
  );
};

export default BookStoreItems;
